services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: wealthtrack
      POSTGRES_USER: wealthtrack
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wealthtrack}
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
